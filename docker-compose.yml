version: '3.8'

services:
  frontend:
    build: .
    container_name: hotel_frontend
    restart: always
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - API_BASE=http://217.216.72.223:3000
      - SESSION_SECRET=docker_secret_key_change_me
      - ADMIN_USER=admin
      - ADMIN_PASS=admin123